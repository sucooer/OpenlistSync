# OpenListSync 部署配置示例
# 复制为 .env 并按需修改:cp .env.example .env
# 注意:.env 含敏感信息,请勿提交到 Git(已在 .gitignore 中忽略)

# Web 界面访问令牌(留空或删除则无需认证;默认仅监听本机,如需对外暴露务必设置)
WEB_API_TOKEN=

# Web 界面监听地址(默认 127.0.0.1:18222 仅本机可访问;容器部署时由 compose 内设为 :18222,此时由端口映射控制暴露范围)
# WEB_LISTEN=127.0.0.1:18222