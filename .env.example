# OpenListSync 部署配置示例
# 复制为 .env 并按需修改:  cp .env.example .env
# 注意: .env 含敏感信息,勿提交到 Git(已在 .gitignore 中忽略)

# Web 界面访问令牌。
# 默认仅监听本机 127.0.0.1:18222 时可以留空(/docker-compose.yml ports 项改为 "127.0.0.1:18222:18222")。
# 当宿主端口 18222 对公网/局域网可达时必须设置,否则容器入口会拒绝启动。
# 推荐: openssl rand -hex 32   (生成 64 个十六进制字符的随机串,熵 256 bit)
WEB_API_TOKEN=

# Web 界面监听地址(默认 127.0.0.1:18222 仅本机可访问)。
# 仅在以下两种情况下需要改:
#   1) 你想在一个不同于 18222 的端口运行;
#   2) 你跑二进制(不是 docker)且需要对外提供本机何处监听——这都需要同时设置 token。
# docker-compose 里默认会设为 ":18222"(全接口),再由 ports 项控制宿主侧暴露范围。
# WEB_LISTEN=127.0.0.1:18222
